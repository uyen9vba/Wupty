
#include <vector>

struct Site {
	bool ocean; // initialized
	bool coast;
	bool border; // initialized
	float elevation; // initialized
	float moisture;

	Biome biome;
	Point point; // initialized
}
